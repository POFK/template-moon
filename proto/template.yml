$schema: ../../.moon/cache/schemas/template.json
title: buf proto project
description: Create buf proto project for generate apis.
destination: '/proto/[name]'
variables:
  name:
    type: 'string'
    default: ''
    required: true
    prompt: 'Package name?'
